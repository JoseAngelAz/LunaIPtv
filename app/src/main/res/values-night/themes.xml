<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night variant of the launch-window theme. Same AMOLED-black window background. -->
    <style name="Theme.LunaIPtv" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/lunaip_bg</item>
        <item name="android:statusBarColor">@color/lunaip_bg</item>
        <item name="android:navigationBarColor">@color/lunaip_bg</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
